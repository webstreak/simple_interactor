general:
  branches:
    only:
      - master

machine:
  ruby:
    version:
      2.2.3

dependencies:
  pre:
    - gem uninstall bundler -v '1.13.5'
    - ./bin/setup

test:
  override:
    - rake spec
